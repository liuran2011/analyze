#!/usr/bin/make -f
# -*- makefile -*-

%:
	dh $@

override_dh_install:
	dh_install --fail-missing
	install -d $(CURDIR)/debian/report-gen/etc/analyze
	install -d $(CURDIR)/debian/python-analyze/var/log/analyze
	cp -r $(SB_TOP)/etc/report_gen.conf $(CURDIR)/debian/report-gen/etc/analyze/ 

